server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sleeve?characterEncoding=UTF-8&serverTimezone=GMT%2B8
    username: root
    password: x5219438
   # 显示sql语句
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true

